{
  "name": "fdb",
  "version": "1.0.0",
  "private": false,
  "workspaces": [
    "src/packages/**",
    "src/apps/**"
  ],
  "dependencies": {
    "typescript": "^5.9.3"
  },
  "devDependencies": {
    "@biomejs/biome": "2.3.0",
    "bunup": "^0.16.10"
  },
  "scripts": {
    "build": "bunup  --exports",
    "build:dev": "bunup --exports --watch",
    "dev:api": "bun run --bun --watch src/apps/api-testing/src/index.ts",
    "dev:testing": "bun run --bun --watch src/apps/testing/src/index.ts",
    "dev:drive": "bun run --bun --watch src/apps/drive/src/main.tsx"
  }
}