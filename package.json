{
  "name": "fdb",
  "version": "1.0.0",
  "private": false,
  "workspaces": [
    "src/packages/**",
    "src/apps/**"
  ],
  "dependencies": {
    "typescript": "^5.9.3"
  },
  "devDependencies": {
    "@biomejs/biome": "2.3.0",
    "bunup": "^0.16.10",
    "npm-run-all": "^4.1.5"
  },
  "scripts": {
    "build:packages": "bunup  --exports",
    "dev:packages": "bunup --exports --watch",
    "dev:api": "bun --bun run --watch src/apps/api-testing/src/index.ts",
    "dev:testing": "bun--bun run --watch src/apps/testing/src/index.ts",
    "dev:drive": "cd src/apps/drive && bun --bun run vite"
  }
}
